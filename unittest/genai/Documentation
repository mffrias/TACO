 02/02/2026
 During testing, parameters such as:
@ public pure
      @   requires n >= 0;
      @   ensures (n == 0 ==> \result == 1)
      @        && (n > 0 ==> \result == n * specFactorial(n - 1));
      @*/
are not well supported.

Feb 20 2026: ChatGPT generated code and jml specifications have been reviewed and documentation has been added, gemini still to be reviewed.
- Find First Zero Test failed
java.lang.ClassCastException: org.multijava.mjc.JForStatement cannot be cast to org.multijava.mjc.JWhileStatement

- JML specification responsible for failure generated by Chat GPT:
        /*@
          @ loop_invariant 0 <= i && i <= x.length;
          @ loop_invariant (\forall int j; 0 <= j && j < i; x[j] != 0);
          @ decreases x.length - i;
          @*/

- FindInArray Implemented, reviewing how to test the methods.
- Genertae a specification pating from this code, if the code has a big the specification will be bug
Excel - method and tool - bug detected or not

March 3 2026:
Find_inSorted
The compilation is stuck at the translation from Kodkod to CNF.
Probable cause:
 Sortedness check: (\forall int i; start <= i && i < end - 1; arr[i] <= arr[i + 1])
 For an array of size n, this generates O(n) constraints
 Two  checks in postconditions: (\exists int i; start <= i && i < end; arr[i] == x)
 When Alloy translates this to CNF, it must encode all possible combinations, causing exponential blowup.

 GCD div method:
  @ ensures (d > 0 ==> 0 <= \result < d);
  @ ensures (d < 0 ==> d < \result <= 0);

Follow python-like syntax, needed to be edited for compiling


Null pointer exception generated because of an exisiting quantifier that is removed by the test class.
ensures (\exists int q; n == q * d + \result);

Error: java.lang.ClassCastException: ar.edu.jdynalloy.ast.JBlock cannot be cast to ar.uba.dc.rfm.alloy.ast.expressions.AlloyExpression

Metrica de comparacion de OpenAI vs Gemini (abierto a otros modelos)

- Replicar GCD

